# ============ SOLANA CONNECTION ============
RPC_URL=https://api.mainnet-beta.solana.com

# ============ WALLET ============
# Option 1: Base58 private key (recommended for production)
WALLET_PRIVATE_KEY=your_base58_private_key_here

# Option 2: Path to keypair file
# WALLET_PATH=~/.config/solana/id.json

# ============ $KryptCash TOKEN ============
# Your token mint address (get from pump.fun after launch)
TOKEN_MINT=your_token_mint_after_pumpfun_launch

# PumpSwap pool address (auto-created after graduation)
PUMPSWAP_POOL_ADDRESS=your_pumpswap_pool_after_graduation

# ============ STREAMFLOW STAKING ============
# Streamflow staking pool ID (created via setup:staking script)
STREAMFLOW_STAKING_POOL=your_streamflow_pool_id

# ============ STAKING CONFIG ============
# Minimum tokens to stake (1M tokens = 1000000000000 with 6 decimals)
MIN_STAKE_AMOUNT=1000000000000

# Lock period in seconds (0 = no lock)
LOCK_PERIOD_SECONDS=0

# Fee router interval in milliseconds (1 hour = 3600000)
ROUTER_INTERVAL_MS=3600000

# Minimum SOL fees to trigger distribution
MIN_FEE_THRESHOLD_SOL=0.01

# ============ LP SETTINGS ============
# Amount of SOL to add as LP (in SOL)
LP_AMOUNT_SOL=10

# Amount of $KryptCash tokens to add as LP (raw amount with decimals)
LP_AMOUNT_TOKENS=1000000000000
